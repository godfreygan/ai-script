apiVersion: v1
kind: Secret
metadata:
  name: ai-script-secret
  namespace: ai-script
type: Opaque
# WARNING: 以下所有值均为占位符，部署前必须替换为实际 base64 编码后的值。
# 生成命令示例: echo -n 'your-value' | base64
data:
  # MySQL DSN，格式: user:password@tcp(host:port)/dbname?charset=utf8mb4&parseTime=True&loc=Local
  MYSQL_DSN: "BASE64_ENCODED_MYSQL_DSN"

  # Redis 密码（无密码则留空 base64 编码）
  REDIS_PASSWORD: "BASE64_ENCODED_REDIS_PASSWORD"

  # JWT 签名密钥（建议 32 字节以上随机字符串）
  JWT_SECRET: "BASE64_ENCODED_JWT_SECRET"

  # 数据库迁移 DSN（通常与 MYSQL_DSN 相同）
  MIGRATE_DSN: "BASE64_ENCODED_MIGRATE_DSN"
  MIGRATE_SOURCE: "BASE64_ENCODED_MIGRATE_SOURCE"

  # OSS / S3 访问密钥
  OSS_ENDPOINT: "BASE64_ENCODED_OSS_ENDPOINT"
  OSS_ACCESS_KEY: "BASE64_ENCODED_OSS_ACCESS_KEY"
  OSS_SECRET_KEY: "BASE64_ENCODED_OSS_SECRET_KEY"

  # 数据加密密钥
  CRYPTO_KEY: "BASE64_ENCODED_CRYPTO_KEY"

  # 大模型网关
  MODEL_GATEWAY_URL: "BASE64_ENCODED_MODEL_GATEWAY_URL"
  MODEL_GATEWAY_KEY: "BASE64_ENCODED_MODEL_GATEWAY_KEY"
